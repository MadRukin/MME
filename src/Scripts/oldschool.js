//Alte Pfandauswahl

// // Button als Label für ein Select-Dropdown-Menu
	// var btn = document.createElement("button");
	// btn.className = "btn btn-default disabled";
	// btn.id = "pfand" + counter;
	// btn.type = "button";
	// btn.innerHTML = "Art des Pfands";
	// btn.setAttribute("style", "padding: 6px 8px;");


	// group.appendChild(btn);

	// // Button zum Dropdown-Toggle
	// btn = document.createElement("button");
	// btn.type = "button";
	// btn.className = "btn btn-default dropdown-toggle";
	// btn.setAttribute("data-toggle", "dropdown");
	// btn.setAttribute("aria-expanded", "false");
	// btn.setAttribute("style", "padding: 6px 8px;");

	// // Dropdown-Icons
	// var e = document.createElement("span");
	// e.className = "caret";

	// btn.appendChild(e);

	// e = document.createElement("span");
	// e.className = "sr-only";
	// e.innerHTML = "Toggle Dropdown";

	// btn.appendChild(e);

	// group.appendChild(btn);

	// Dropdown-Menu
	// var ul = document.createElement("ul")
	// ul.className = "dropdown-menu";
	// ul.setAttribute("role", "menu");

	// li = document.createElement("li");
	// li.className = "pfand-auswahl";
	// a = document.createElement("a");
	// a.innerHTML = "Bierflaschen 0.5l";
	// a.name = "Bierflaschen 0.5l";

	// li.appendChild(a);

	// ul.appendChild(li);

	// li = document.createElement("li");
	// li.className = "pfand-auswahl";
	// a = document.createElement("a");
	// a.innerHTML = "Bierflaschen 0.33l";
	// a.name = "Bierflaschen 0.33l";

	// li.appendChild(a);

	// ul.appendChild(li);

	// li = document.createElement("li");
	// li.className = "pfand-auswahl";
	// a = document.createElement("a");
	// a.innerHTML = "Mischgetränke 0.5l (Mate, Spezi etc.)";
	// a.name = "Mischgetränke 0.5l";

	// li.appendChild(a);

	// ul.appendChild(li);

	// li = document.createElement("li");
	// li.className = "pfand-auswahl";
	// a = document.createElement("a");
	// a.innerHTML = "Dosenpfand/PET-Pfandflaschen";
	// a.name = "PET/Dosen";

	// li.appendChild(a);

	// ul.appendChild(li);